import Image from "next/image";
import TextDemoBlock from "./text-demo-block";
import LayoutText from "./layout-text";

const BlockDemoText = () => {
  return (
    <LayoutText>
      <TextDemoBlock />
    </LayoutText>
  );
};

export default BlockDemoText;
